import React from 'react'
import ReactMarkdown from 'react-markdown'

export default function PrismaTutorial(){
  return(
    
    <ReactMarkdown>*React-Markdown* is 
      ```
      <p></p>
      ```
      </ReactMarkdown>
  )
}